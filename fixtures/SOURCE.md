# Fixture source

`pre/` is clean and matches `intents/demo.yaml`.
`post/` plants adjacency loss, missing routes, interface errors, and config drift.

Replace these with a live capture when you have a lab. See docs/LIVE.md.
